import * as plugins from './serverconfig.plugins';

export class Stack {
  public static async getAvailableStacks(): Promise<Stack[]> {
    const resultArray: Stack[] = [];
    return resultArray;
  }

  
  public stackFolder: string;

  constructor() {};
}
